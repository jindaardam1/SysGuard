<UserControl x:Class="SysGuard.Benchmark"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#444444">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="30" FontWeight="Bold" Foreground="Gray"
                   TextWrapping="Wrap" Width="510" TextAlignment="Center" Margin="0 0 0 30">
            Los benchmarks estarán disponibles a partir de la versión 1.1.0
            </TextBlock>
            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="30" FontWeight="Bold" Foreground="Gray"
                       TextWrapping="Wrap" Width="510" TextAlignment="Center">
                <Run Text="Desde el equipo de "/>
                <Run Text="SysGuard" Foreground="#B8D600"/>
                <Run Text=" estamos trabajando para tenerlos lo antes posible."/>  
            </TextBlock>

        </StackPanel>
    </Grid>
</UserControl>
